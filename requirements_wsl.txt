# WSL Requirements for Paradex Market Making Bot
# This file contains the dependencies needed to run the bot in WSL with the official Paradex SDK

# Official Paradex SDK (this is the key difference from Windows)
paradex-py

# Core dependencies
aiohttp>=3.8.0
PyYAML>=6.0
python-dotenv>=1.0.0

# Optional: Additional utilities
requests>=2.28.0
asyncio-mqtt>=0.11.0  # If you want to add MQTT support later

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0

